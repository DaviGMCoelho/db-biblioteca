/* MOSTRAR USUARIOS DO SISTEMA (LISTA USUARIOS)*/
SELECT p.NOME, p.CPF, p.COD_PERFIL, p.DATA_NASCIMENTO, p.EMAIL, p.TELEFONE, s.STATUS
	FROM PERFIS p
	INNER JOIN STATUS_USUARIO s
		ON p.ID_STATUS = s.ID_STATUS
