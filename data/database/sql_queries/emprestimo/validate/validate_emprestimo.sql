/* Verifica se tem emprestimos ativos do usuário*/
SELECT 1 FROM EMPRESTIMOS
WHERE ID_PERFIL = (
	SELECT ID_PERFIL FROM PERFIS
	WHERE COD_PERFIL = @COD_PERFIL) 
AND STATUS = (
    SELECT ID_STATUS FROM STATUS_EMPRESTIMO WHERE STATUS = @STATUS
)