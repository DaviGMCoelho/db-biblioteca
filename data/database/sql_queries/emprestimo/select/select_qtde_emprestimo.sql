/* Retorna a quantidade de emprestimos com status desejado */
SELECT COUNT(*) AS TOTAL_EMPRESTIMOS FROM EMPRESTIMOS 
	WHERE ID_STATUS = (SELECT ID_STATUS FROM STATUS_EMPRESTIMO WHERE STATUS = @STATUS)