/* Mostra os usuários do sistema por tipo desejado */
SELECT NOME, CPF, COD_PERFIL, DATA_NASCIMENTO, EMAIL, TELEFONE
	FROM PERFIS
	WHERE ID_TIPO = (SELECT ID_TIPO FROM TIPOS_USUARIO WHERE TIPO = @TIPO_USUARIO)
